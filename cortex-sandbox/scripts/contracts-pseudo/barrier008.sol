pragma solidity ^0.5.0;

contract Barrier000 {
  function send(address payable _receiver) public payable {
    _receiver.send(msg.value);                "Represent the mechanics of very primitive PRNGs.",
                                              """ VAR seed = HASH(ADDRESS_SELF + START_TIME)
                                                | if (seed == HASH(CALL_DATA[1])) {
                                                |     HALT(WINNER)
                                                | }""".trimMargin(),
  }

  function setup() public returns (string memory thanks) {
    return "thanks";
  }
}
